{
  "name": "Computer Systems Security - nmpap lab",
  "dockerComposeFile": "docker-compose.yml",
  "service": "victim",  // specify the primary service you want to attach to
  "workspaceFolder": "/workspace",
  "extensions": [
    "ms-vscode.cpptools",
    "ms-vscode.cmake-tools"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  }
}
